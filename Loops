#The provided code stub reads and integer,n , from STDIN. For all non-negative integers i<n, print i sqr

if __name__ == '__main__':
    n = int(input())
    for i in range (0,n):
        print(i*i)
        i=i+1
        
#LeapYear 
def is_leap(year):
  leap=False
  
  if(year%4==0 and (year%100!=0 or year%400==0)):
    return True
  else:
    return leap
    
year = int(input())
print(is_leap(year))
